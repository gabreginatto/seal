# PNCP Data Processor - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# PNCP API Credentials (Required)
# Obtain these from PNCP portal registration
# These credentials are shared between medical and lacre systems
PNCP_USERNAME=your_pncp_username
PNCP_PASSWORD=your_pncp_password

# Google Cloud Configuration (Required)
# Your GCP project ID (shared between medical and lacre)
GOOGLE_CLOUD_PROJECT=your-project-id

# Cloud SQL instance details (same instance for both databases)
CLOUD_SQL_REGION=us-central1
CLOUD_SQL_INSTANCE=pncp-medical-instance

# Medical Database Configuration
DATABASE_NAME=pncp_medical_data

# Lacre Database Configuration
# Uses same Cloud SQL instance but different database
LACRE_DATABASE_NAME=pncp_lacre_data

# Database authentication (choose one method)
# Option 1: Use IAM authentication (recommended)
# Ensure your service account has Cloud SQL Client role
# No additional credentials needed

# Option 2: Use database user/password
# DB_USER=postgres
# DB_PASSWORD=your_secure_password

# Product Catalog Configuration (Optional)
# Path to Fernandes medical product catalog CSV
FERNANDES_CATALOG_CSV=/path/to/fernandes_products.csv

# Path to lacre product catalog CSV
LACRE_CATALOG_CSV=/path/to/lacre_products.csv

# Processing Configuration (Optional)
# USD to BRL exchange rate for price comparisons
USD_TO_BRL_RATE=5.0

# API Rate Limiting (Optional)
MAX_REQUESTS_PER_MINUTE=60
MAX_REQUESTS_PER_HOUR=1000

# Logging Configuration (Optional)
LOG_LEVEL=INFO
LOG_FILE=pncp_processing.log

# Export Configuration (Optional)
EXPORT_DIRECTORY=exports
REPORT_DIRECTORY=reports

# Notification Settings (Optional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
# EMAIL_NOTIFICATIONS=admin@yourcompany.com